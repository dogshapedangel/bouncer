DISCORD_TOKEN=
DISCORD_CLIENT_ID=
# Optional: use a test guild for instant command updates.
DISCORD_GUILD_ID=
# Optional path to data file.
DATA_FILE=./data/blocked-users.json
# Optional: channel ID for action logs (bounce/unbounce/join enforcement events).
ACTION_LOG_CHANNEL_ID=
# Optional: whether to remove bounced users' system join messages.
# true (default) or false
REMOVE_JOIN_MESSAGES_ON_BOUNCE=true
